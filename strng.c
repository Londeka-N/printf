#include "main.h"
/**
 * _strlen - a function that finds te length of a string
 * @s: te string whose lenght neds to be checked
 *
 * Return: the length of the string or number of characters
*/

int _strlen(char *s)
{
	int length = 0;

	while (s[length] != '\0')
	{
		length++;
	}

	Return lengh;
}
